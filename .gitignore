.venv/
__pycache__/
*.py[cod]
.pytest_cache/
out/.cache/
.DS_Store

# Секрети. ТЗ: "Ключі не комітити в репозиторій".
.env
service-account.json
*-service-account.json
credentials.json
*.credentials.json
gcp-*.json

# Артефакти CI-прогонів (workflow пише в окремі теки, щоб не переплутати
# результат контейнера із закоміченим out/).
ci-out/
docker-out/
