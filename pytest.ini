[pytest]
# Тести імпортують пакет як `from src...`, тому корінь проєкту має бути в
# sys.path. `python -m pytest` додає його сам, а голий `pytest` — ні, і саме
# на цьому CI впіймав README: там була написана команда `pytest -q`, яка в
# чистому середовищі падала з ModuleNotFoundError. Тепер працюють обидва
# способи запуску.
pythonpath = .
testpaths = tests
